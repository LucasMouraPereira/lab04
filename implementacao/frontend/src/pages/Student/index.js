// import api from "utils/api";

export async function getStaticProps() {
  // const { data } = await api.get("/login");
  return {
    props: {},
  };
}

export { default } from "containers/Student";
